<section class="secondary">
    <div class="col-8 offset-2">
        <form id="newUserRegistration" action="/register" method="POST">
            <div class="form-group row" style="font-size:24px;font-weight:700">New User Registration</div>
            <div class="form-group fName">
                <label for="regist_firstName">First Name</label>
                <input type="text" name="firstName" class="form-control" id="regist_firstName" placeholder="First Name">
            </div>
            <div class="form-group lName">
                <label for="inputLastName">Last Name</label>
                <input type="text" name="lastName" class="form-control" id="inputLastName" placeholder="Last Name">
            </div>
            <div class="form-group mail">
                <label for="inputEmail">Email address</label>
                <input type="text" name="email" class="form-control" id="inputEmail" placeholder="Enter email">
                <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone
                    else.</small>
            </div>
            <div class="form-group">
                <label for="inputPassword">Password</label>
                <input type="password" name="password" class="form-control" id="inputPassword" placeholder="Password">
            </div>
            <input type="submit" class="btn btn-primary btn-lg"></button>
        </form>
    </div>
</section>

<script src="https://code.jquery.com/jquery-3.4.1.js" integrity="sha256-WpOohJOqMqqyKL9FccASB9O0KwACQJpFTUBLTYOVvVU="
    crossorigin="anonymous"></script>

<script type='text/javascript'>



    $('#newUserRegistration').submit(function (event) {
        event.preventDefault();
        var firstName = event.target.firstName.value;
        var lastName = event.target.lastName.value;
        var email = event.target.email.value;
        var pw = event.target.password.value;
        alert(hi)
        if (firstName.length < 1 || firstName.length > 50) {
            $("input:eq(0)").css("border", "red solid 2px");
            $('.fName').append("<small class="color: red">required *</small>");
        } else if (lastName.length < 1 || lastname.length > 50) {
            $("input:eq(0)").css("border", "red solid 2px");
            $('.lName').append("<small class="color: red">required *</small>");
        } else if (email === '') {
            $('.mail').append("<small class="color: red">required *</small>");
        } else {
            alert(`Form submitted, Please check your email !  Your details are:\n\nfirstname: ${firstname}\nlastname: ${lastName}\nemail: ${email}\npassword: ${pw}`);
        }
    });
</script>
